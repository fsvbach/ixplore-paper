"""One script per figure of the paper.

Every module exposes ``main()`` and writes its PDF to ``figures/`` from the
stored ``results/``. Run one figure with ``python -m src.figures.<name>`` or
all of them with ``python -m src.figures``.
"""
